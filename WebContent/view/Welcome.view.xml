<mvc:View
	controllerName="sap.ui.demo.walkthrough.controller.ShoppingCart"
	xmlns:mvc="sap.ui.core.mvc" 
	xmlns="sap.m">
	<Page
		id="page"
		title="{i18n>welcomePageTitle}"
		backgroundDesign="Solid"
		showNavButton="{appView>/smallScreenMode}">
		<Text text="1: order form does not start from step 1 until hard refresh of page." />
		<Text text="2: sum of price does not show in cart but it is calculated" />
		<Text text="" />
	</Page>
</mvc:View>
